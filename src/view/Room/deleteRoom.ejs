<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Delete Room</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            margin-top: 80px;
            padding-left: 260px;
            overflow-x: hidden;
        }
    </style>
</head>

<body>

    <%- include('../Reception/receptionNavbar') %>
        <%- include('../Reception/receptionSidebar') %>

            <div class="container mt-5">
                <div class="card shadow p-4">
                    <h2 class="mb-4 text-center text-danger">Delete Room - <%= room.room_no %>
                    </h2>

                    <p>Are you sure you want to delete this room?</p>

                    <ul class="list-group mb-3">
                        <li class="list-group-item"><strong>Type:</strong>
                            <%= room.room_type %>
                        </li>
                        <li class="list-group-item"><strong>Status:</strong>
                            <%= room.room_status %>
                        </li>
                        <li class="list-group-item"><strong>Price:</strong> ₹<%= room.room_price %>
                        </li>
                    </ul>

                    <form action="/receptionist/delete-room/<%= room.room_no %>" method="POST">
                        <button type="submit" class="btn btn-danger w-100 mb-2">Yes, Delete</button>
                        <a href="/receptionist/view-rooms" class="btn btn-secondary w-100">Cancel</a>
                    </form>
                </div>
            </div>

            <%- include('../fotter') %>

                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>