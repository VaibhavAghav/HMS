<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Room</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            margin-top: 80px;
            padding-left: 260px;
            overflow-x: hidden;
        }
    </style>
</head>

<body>

    <%- include('../Reception/receptionNavbar') %>
        <%- include('../Reception/receptionSidebar') %>

            <div class="container mt-5">
                <div class="card shadow p-4">
                    <h2 class="mb-4 text-center">Edit Room - <%= room.room_no %>
                    </h2>

                    <form action="/receptionist/edit-room/<%= room.room_no %>" method="POST">


                        <div class="mb-3">
                            <label class="form-label">Room Number</label>
                            <input type="text" class="form-control" name="room_no" value="<%= room.room_no %>"
                                required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Room Type</label>
                            <input type="text" class="form-control" name="room_type" value="<%= room.room_type %>"
                                required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Room Status</label>
                            <input type="text" class="form-control" name="room_status" value="<%= room.room_status %>"
                                required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Room Price (₹)</label>
                            <input type="number" step="0.01" class="form-control" name="room_price"
                                value="<%= room.room_price %>" required>
                        </div>

                        <button type="submit" class="btn btn-success w-100">Update Room</button>
                    </form>
                </div>
            </div>

            <%- include('../fotter') %>

                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>