<!DOCTYPE html>
<html>

<head>
    <title>Edit Doctor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <div class="container mt-5">
        <h2>Edit Doctor Details</h2>
        <form action="/admin/edit-doctor/<%= doctor.doctor_id %>" method="POST">
            <div class="mb-3">
                <label>Name</label>
                <input type="text" name="doctor_name" class="form-control" value="<%= doctor.doctor_name %>" required>
            </div>
            <div class="mb-3">
                <label>Contact</label>
                <input type="text" name="doctor_contact" class="form-control" value="<%= doctor.doctor_contact %>"
                    required>
            </div>
            <div class="mb-3">
                <label>Experience (years)</label>
                <input type="number" name="doctor_experience" class="form-control"
                    value="<%= doctor.doctor_experience %>" required>
            </div>
            <div class="mb-3">
                <label>Specialization</label>
                <input type="text" name="spelization" class="form-control" value="<%= doctor.spelization %>" required>
            </div>
            <div class="mb-3">
                <label>Status</label>
                <select name="status" class="form-select" required>
                    <option value="Active" <%=doctor.status==='Active' ? 'selected' : '' %>>Active</option>
                    <option value="Inactive" <%=doctor.status==='Inactive' ? 'selected' : '' %>>Inactive</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Update Doctor</button>
            <a href="/admin/view-doctors" class="btn btn-secondary">Cancel</a>
        </form>
    </div>

</body>

</html>