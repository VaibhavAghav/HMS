<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Reception Panel - Global Care Hospital</title>
    <!-- Bootstrap 5 CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            overflow-x: hidden;
        }

        .sidebar {
            width: 250px;
            background-color: #f8f9fa;
            min-height: 100vh;
        }

        .navbar {
            position: sticky;
            top: 0;
            z-index: 1030;
        }

        .main-content {
            flex-grow: 1;
            padding: 20px;
        }
    </style>
    <link rel="stylesheet" href="/css/style.css" />
</head>

<body>

    <!-- Navbar -->
    <%- include('receptionNavbar.ejs') %>

        <div class="d-flex" style="min-height: calc(100vh - 56px);">

            <!-- Sidebar -->
            <div class="sidebar shadow">
                <%- include('receptionSidebar.ejs') %>
            </div>

            <!-- Main content -->
            <div class="main-content d-flex flex-column">

                <div class="container-fluid my-4">

                    <div class="card shadow-lg p-4 bg-light">
                        <div class="card-body text-center">
                            <h1 class="mb-4">Welcome, <%= reception.reception_name %>!</h1>
                            <p class="lead mb-4">Manage your hospital team and patient records efficiently.</p>
                        </div>
                    </div>

                    <div class="card mt-4 shadow-sm">
                        <div class="card-header bg-primary text-white">
                            <h4>Your Profile Details</h4>
                        </div>
                        <div class="card-body">
                            <ul class="list-group mb-3">
                                <li class="list-group-item"><strong>Reception ID:</strong>
                                    <%= reception.reception_id %>
                                </li>
                                <li class="list-group-item"><strong>Name:</strong>
                                    <%= reception.reception_name %>
                                </li>
                                <li class="list-group-item"><strong>Contact:</strong>
                                    <%= reception.reception_contact %>
                                </li>
                                <li class="list-group-item"><strong>Username:</strong>
                                    <%= reception.username %>
                                </li>
                            </ul>

                            <h5 class="mt-4">Additional Information:</h5>
                            <ul class="list-group">
                                <li class="list-group-item">Assigned Shifts: <span class="text-muted">Morning,
                                        Evening</span></li>
                                <li class="list-group-item">Patients Registered Today: <span class="text-muted">8</span>
                                </li>
                                <li class="list-group-item">Appointments Scheduled: <span class="text-muted">5</span>
                                </li>
                            </ul>
                        </div>
                    </div>

                </div>

                <!-- Footer -->
                <%- include('receptionFotter') %>

            </div>

        </div>

</body>

</html>